export function GenerateMap(generatorType: string) {

    switch (generatorType){
        default:
            break;
    }
    
    console.log("Generating map... with generator type: " + generatorType);
}